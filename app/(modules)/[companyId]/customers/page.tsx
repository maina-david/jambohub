import React from 'react'

export const metadata = {
  title: "Customers",
}

function CustomersPage({ params }: { params: { slug: string } }) {
  return (
    <div>CustomersPage</div>
  )
}

export default CustomersPage
