import React from 'react'

export const metadata = {
  title: "Chats",
}

function ChatsPage({ params }: { params: { slug: string } }) {
  return (
    <div>ChatsPage</div>
  )
}

export default ChatsPage
